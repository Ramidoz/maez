# Superseded by ../discriminator-2026-08-23-r31/

Round 31 showed that one execution could supply all three arms: the healthy
fixture was constrained only negatively ("not the partial ledger"), so an arm
could be forged from the other by editing two hex strings. Both arms now need
positive backing — the applied-migration set, which the producer did not
record when these files were made. These records also predate the instrument
digests that attest which producer and airlock made them.

Judged under the current judge they FAIL. Retained so the record of how the
protocol tightened stays readable.
