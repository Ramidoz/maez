# Valence v0.2 — Codex Build Brief (Claude → Codex)

**Lane:** Codex builds, Claude reviews (covenant axis on the review side — same split as Novelty Harbor). Owner: Rohit.

**Branch:** `valence-v0.2` @ `ab382bb` (from main `62e2c8a`, local-only/unpushed).
**Spec:** `docs/superpowers/specs/2026-06-10-valence-v0.2-design.md` (@ `b578811`).
**Plan (complete TDD code):** `docs/superpowers/plans/2026-06-10-valence-v0.2.md` — build this task-by-task.

**Test runner:** `/home/rohit/maez/.venv/bin/python -B -m unittest` (NOT pytest). Strict TDD: write the failing test, verify RED, minimal implement, verify GREEN, `&&`-gate each commit on green.

## What this slice is
Resolved-only: wire the `satisfied`-events delta into valence's `resolved` so the live thermometer gains its POSITIVE want-progress channel. `blocked`/`stale` stay `0` and are explicitly marked `not_live_derived` — they have no honest event source and are deferred by design. The pure `core/evolution/valence/` package is **not touched** (it already turns `resolved>0` into `Sign.POSITIVE`).

## Build constraints (beyond the plan's per-step code)
- **STOP before merge.** No merge, no restart, no live witness. Those are owner breaths after Claude's review passes.
- **`## Predicted effect`** goes on the Task 4 daemon commit only (it's the behavior-affecting one); the others are plain `feat`/`test`/`docs`.
- **Two existing assertions must be updated** in `tests/test_valence_live_wiring.py` (their daemons have no `wants`, so the call gains `resolved=0`) — the plan shows the exact edits. Do not weaken them; just add `resolved=0`.
- **Daemon refinement (intentional):** check `getattr(daemon, "wants", None)` **before** reading the cursor, so no-wants callers never touch the log (keeps existing tests hermetic). The plan's Task 4 code reflects this; it is identical in behavior to the spec snippet.
- Co-author trailer on every commit: `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>` (and your own as appropriate).
- Task 5 writes the review handoff (`docs/handoffs/2026-06-10-valence-v0.2-for-review.md`) and reports branch tip + verification outputs.

## Claude's review anchors (the acceptance contract — build to satisfy these)
1. `Wants.count_events_since(...)` is **read-only** (a `SELECT COUNT`) and **validates `event_type`** against the known types (`ValueError` on unknown) — not a raw-SQL escape hatch.
2. The ISO valence timestamp is **converted to epoch** before querying `want_events.ts` (the two clocks differ: `want_events.ts` is `time.time()` float, valence log `ts` is ISO). A test pins the conversion against a known pair.
3. **No direct daemon SQL** against `want_events` — the daemon asks the wants organ via `count_events_since`, reusing the existing `daemon.wants` object (no new `Wants(...)` in the heartbeat path).
4. `valence_live` still **imports no `wants`** (the Task 3 boundary test pins it; the cursor reader only reads valence_live's own log).
5. `blocked`/`stale` remain `0` and explicitly `not_live_derived` in the `want_coverage` record marker (never faked as measured-zero).
6. A **wants-read failure degrades to `resolved=0`** and never raises into the heartbeat (the gather is wrapped in its own try; the outer read stays heartbeat-safe and wake-gated).
7. Witness (owner, after merge/restart): satisfy a test want → the next woken cycle reads **POSITIVE** want-progress.

## After build → review → owner breaths
Codex builds & stops → Claude covenant-reviews against the seven anchors → owner: merge (local ff, no push) → owner restart → owner witness (anchor 7).
