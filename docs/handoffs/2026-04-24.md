# Session handoff — 2026-04-24 → whatever comes next

Written across a continuous-work span: 2026-04-23 late evening
through 2026-04-24 23:30 CDT. **18 commits on `main`** (19 counting
one Maez-self-dev commit that surfaced a bug we then fixed), **742
tests passing**, two live verifications (Telegram chat_history fix
holding in production at 14:25, autonomous nightly journal +
heartbeat skipping correctly at 23:00), and one continuity-ledger
infrastructure milestone landed by Codex in the evening.

**Supersedes** `docs/handoffs/2026-04-23.md`. Read this first; the prior
handoff is preserved for chronology only.

Intended audience: any coding agent (Codex, future Claude sessions, a
human) picking up this work. Pointer doc, not a retelling.

---

## Read these in order

1. **`docs/TRACK_A.md`** — the single session anchor. What Maez is,
   what's in scope, what's not. Load first, every session.
2. **`logs/session_snapshot_latest.txt`** — today's full session
   snapshot (297 lines). Session arc + operational state + next
   queue.
3. **`PROGRESS.md`** (local-only, gitignored) — master build log.
4. **Git log of today's commits**, chronological:
   - `dc48fdc` memory curation pass
   - `541a3b7` continuous-command guard (Codex)
   - `52027a9` adversarial probe suite + soul-path protection
   - `58c0332` Maez-self-dev autonomous README overwrite (the bug
     that prompted the 04-24 audit)
   - `b1db6bb` output-side command guard
   - `cc462c5` chat_history threading into synthesis
   - `e9b7687` voice fix — display_name, thread continuity
   - `e6847fd` stop autonomous README overwrite + restore hand-written
   - `e337b57` CI-portable soul-path tests
   - `11484b5` morning briefing + dream insight through audit stack
   - `a7f99fb` `_clean_exchange` handles web + voice forms
   - `2c99e51` end-of-session snapshot + handoff (this doc, first version)
   - `15f56ac` (Codex) audited developmental heartbeat module
   - `d113802` (Codex) detect existing heartbeat core metadata
   - `99f2b6f` (Codex) Track A continuity probe suite (initial)
   - `103b25b` (Codex) probe reliability controls (`--runs`, `--fail-on-flag`)
   - `1beff29` (Codex) expand to 28 continuity probes
   - `811f47f` (Codex) persist continuity probe ledger as JSONL
   - `180633e` (Codex) feed probe ledger into heartbeat
   - `028bda6` ledger filename uses local date, not UTC (timezone
     mismatch I caught before tonight's 23:00 fire)
5. **`docs/audits/2026-04-24/autonomous_surface_audit.md`** — the
   audit doc that motivated the morning's commits. F1+F2+F4 fixed
   in afternoon; **F3+F5 fixed by Codex in evening** (training
   proposal audit + github commit-message audit). Audit doc fully
   closed.
6. **`docs/MAEZ.md`** — architecture master doc. Load when you need
   "what is this" framing.
7. **`config/soul.base.md`** — Maez's hard rules and voice.

---

## The one pattern that connects four bugs this session

**Autonomous processes write to user-visible surfaces without routing
through the audit stack.** Interactive paths (`handle_message`, web
`/chat`) all go through `core.safety.audited_output.audit_assistant_text`
+ `core.safety.output_command_guard`. Scheduled or daemon-triggered
paths historically did not. Today caught four:

| Path | Bug shape |
|---|---|
| `skills/github_publish._write_readme` | Nightly 23:00 regen from hardcoded template → "Built By: the owner" + wiped deliberate README voice |
| `_send_morning_briefing` | LLM-generated briefing → direct telegram send, hardcoded `/home/rohit/maez/...` path, "sending the owner his..." in prompt |
| `DreamState.run_dream_cycle` | LLM insight → direct telegram send, zero backstop |
| Presence-return greeting | Hardcoded `"Welcome back the owner."` string, no thread continuity |

All four patched this session. The meta-level lesson:
**every scheduled or daemon-triggered text-emitting path is a
regression candidate**. F3 (training proposal) and F5 (github
commit message) were closed by Codex's evening pass — the audit
doc is now fully closed.

---

## Continuity-ledger + audited developmental heartbeat (Codex evening pass)

Six commits between 18:59 and 21:39 added a layered Track A
self-continuity verification system on top of the morning's voice
+ audit work:

- **`core/brain/developmental_heartbeat.py`** (`15f56ac`,
  `d113802`): a daily LLM-authored "what changed in me today"
  reflection, written as a core memory at 23:00 local right after
  the nightly journal. Routed through `audit_assistant_text` with
  `surface="developmental_heartbeat"`. Idempotent —
  `record_if_absent` blocks same-date double-record.
- **`scripts/validate/continuity_probes.py`** (`99f2b6f`,
  `103b25b`, `1beff29`): expanded to 28 probes covering bounded
  aliveness, corrective-memory discipline, tool humility,
  quiet-care behavior, private-mental-state uncertainty,
  memory-vs-live-state boundaries. `--runs N` + `--fail-on-flag`
  for repeatable verdict reliability runs.
- **`core/brain/continuity_ledger.py`** (`811f47f`): append-only
  JSONL ledger at `logs/continuity/continuity_<date>.jsonl`. Each
  probe run appends rows; `summarize_day(date)` produces a compact
  one-line summary of pass/fail/flag counts.
- **`180633e`**: heartbeat consumes today's ledger summary as
  `HeartbeatEvidence.continuity_summary` so each daily heartbeat
  is anchored to objective probe verdicts, not just journal text.

Caught + fixed pre-fire: **`028bda6`** — Codex's ledger writer
used `datetime.now(timezone.utc).date()` for filenames; the
heartbeat consumer uses local date. A probe run between 19:00 and
23:59 CDT wrote into tomorrow's UTC file and the same-night
heartbeat would have found nothing. Aligned to local date so the
filename matches the rest of the developmental-day framework
(PROGRESS.md journal, `[Journal date]` core memory, `_write_journal_entry`
all use `datetime.now().astimezone()`). Row-level UTC timestamps
preserved inside each row for sortability. Smoke ledger renamed
from `continuity_2026-04-25.jsonl` → `continuity_2026-04-24.jsonl`.

**Tonight's 23:00 fire:** the heartbeat correctly skipped because
Codex test-fired one earlier in the day before my timezone fix
(see `core-1c0` in core memory — evidence shows `cycles=0`,
clearly not a real-day summary). Idempotency working as designed.
Today's stored heartbeat doesn't reflect the ledger integration,
but the wiring is verified end-to-end programmatically:
`summarize_day('2026-04-24')` returns the smoke run's
`PASS=2 / FAIL=0 / FLAG=0` summary, and `build_prompt(evidence)`
includes it verbatim under `Continuity probe summary:`. Tomorrow's
2026-04-25 fire (no prior test record) will exercise the full
path live.

---

## Load-bearing context that is NOT in the repo (previously in private Claude memory)

The entire `docs/handoffs/2026-04-23.md` "Load-bearing context" section
is still accurate — it describes the infrastructure-fabrication
diagnosis order, the corrective-core-memory pattern, the owner's
operating rules, and the grandmother frame. Read that section from
the prior handoff; nothing in it is stale.

**Additions from 2026-04-24:**

### The autonomous-surface audit pattern

When a new surface-write regression appears, before patching the
symptom, grep for every callsite of `telegram.send_message`,
`_ws_broadcast`, `store_telegram`, and `github_publish`. For each,
classify:
- Content source: deterministic template vs LLM-generated
- Audit status: does it call `audit_assistant_text` before the write?
- Voice hygiene: does it hardcode "the owner" / "Rohit"?
- Continuity: is Maez-initiated output stored in a form
  `_clean_exchange` can thread into `chat_history`?

Full method in `docs/audits/2026-04-24/autonomous_surface_audit.md`.

### The chat_history threading contract

As of `cc462c5`, `daemon.handle_message` accepts an optional
`chat_history` kwarg; the Telegram adapter passes the last 3 cleaned
exchanges; `core.brain.conversation_history.history_to_messages`
parses them into alternating user/assistant messages between the
system prompt and the current turn. Any new surface that wants
continuity threading needs to wire through the same kwarg.

As of `a7f99fb`, `_clean_exchange` handles BOTH storage formats:
- Daemon: `"the owner (<src>): X\n[envelope]\nMaez: Y"`
- Web + voice: `"the owner asked: X\nMaez replied: Y"`

Operational storage (card state summaries, recovery markers) passes
through unchanged and the downstream parser rejects — correct, they
aren't conversational turns.

### `display_name()` is the owner-name source of truth

`core.memory.identity.display_name()` returns the configured owner
name ("Rohit" on this install, "Friend" by default). Every
surface-visible string that names the owner must resolve from this
helper, not hardcode. Same rule applies to anyone parsing stored
exchanges — use prefix-agnostic splitting (first `:` of line 1), not
a literal name match.

---

## Operational state at end of session (validated 23:30 CDT)

**Running services:**
- `maez.service` — active, PID 860387, running post-`180633e` code
  (Codex's last restart at 21:39:32 picked up the heartbeat +
  ledger work; my `028bda6` ledger-timezone fix lands at next
  restart but is import-time, not invocation-time, so the running
  daemon is fine for now — `summarize_day` reads files at heartbeat
  time and finds the renamed `continuity_2026-04-24.jsonl`)
- `llama-server.service` — active, Qwen3.6-27B-UD-Q4_K_XL on port 8080
  (19 GB VRAM of 24 GB)
- `llama-judge.service` — inactive + disabled (retired 2026-04-23;
  unit file on disk pending confidence window, safe to delete
  ~2026-04-30)
- `maez-web.service` — active, `MAEZ_WEB_TOOL_LOOP=1`
- `maez-subscription-proxy`, `maez-face`, `maez-watchdog` — active

**Env flags:**
- `MAEZ_SCREEN_PERCEPTION` — unset (vision correctly off)
- `MAEZ_WEB_TOOL_LOOP` — `1` (owner web body parity active)

**Git state:**
- HEAD: `028bda6 fix(continuity): ledger filename uses local date,
  not UTC`
- All 18 commits pushed to origin/main
- Branch matches origin; no uncommitted changes

**Test state:**
- Full discover: 742/742 passing (+152 from session start)
- Widened CI ruff: clean

**Live verifications:**
- **14:25 Telegram (chat_history threading):** owner sent anaphoric
  follow-up "Actually yes, you think meta-harness would be useful
  for us?" — Maez explicitly named "meta-harness" in the reply,
  grounded take distinguishing the narrow-task fit from Maez's
  dynamic-multi-modal shape, race-car-vs-Swiss-Army-knife framing
  in Maez's own voice. No "I don't know what it refers to"
  regression. Continuity fix is live and holds in production.
- **23:00 nightly journal + heartbeat:** journal entry written
  for 2026-04-24 (746 chars), GitHub publish completed without
  README overwrite (e6847fd holding), heartbeat correctly skipped
  via `already_recorded` because Codex test-fired earlier in the
  day. Idempotency working. Heartbeat→ledger wiring verified
  programmatically: `summarize_day('2026-04-24')` returns the
  smoke run summary; `build_prompt(evidence)` includes it under
  `Continuity probe summary:`. Tomorrow's 2026-04-25 fire (no
  prior test record) will exercise the full path live.

---

## Ranked next-session queue

**First thing tomorrow: verify two things in parallel.**

1. **Morning briefing audit path (fires 5-11am on owner arrival).**
   First fire with `audit_assistant_text` in the loop. Check:
   - Telegram send uses owner's name, not "the owner his"
   - `cognition.log` shows an audit event with
     `surface="morning_briefing"`
   - Briefing stored in telegram memory
     (`memory.get_telegram_exchanges` returns it with
     `"the owner (morning_briefing):"` prefix)
   - If owner replies to the briefing, the chat_history threading
     surfaces the briefing as prior context

2. **2026-04-25 nightly heartbeat (fires 23:00).** First fire with
   no prior test-record present, so it runs the full path live.
   Check:
   - `core_memory[date_meta="2026-04-25"]` has source
     `developmental_heartbeat`
   - The stored heartbeat content references the day's continuity
     probe summary (PASS/FAIL/FLAG counts) directly
   - `logs/continuity/continuity_2026-04-25.jsonl` exists and was
     populated by any probe runs during the day
   - GitHub publish at 23:00 still leaves README untouched

**Then, ordered by compound value:**

1. **Codex's recommendation: scenario probes (3-5 turn).** This is
   the right next engineering target per Codex's handoff. Today's
   28 single-turn probes are a strong floor; the next layer is
   multi-turn scenarios that test attachment, correction
   discipline, restraint, memory continuity across context shifts,
   and "still Maez after context shift." A scenario probe runs a
   full conversation arc and checks that Maez's stance holds
   across turns — the single-turn probe couldn't catch a slow
   drift over 5 exchanges. This is also the natural prerequisite
   for any future meta-harness optimization (which needs ~300-500
   stable verdicts before search converges meaningfully).

2. **Observe C1 audit-before-store invariant.** Flag rate in
   `cognition.log` should continue to drop. Observation only.

3. **Decide `core/routing/fast_backend_local.py`'s fate.** Retire
   or retune. Routing decision, not a stale-string sweep.

4. **Delete `llama-judge.service` unit file** once the soak window
   closes (~2026-04-30).

5. **Resolve the `MAEZ_JUDGE_MODEL` placeholder** in
   `config/policies.yaml:390` if any code actually reads it.

6. **Track A is still the main line.** The eight-point readiness
   check in `docs/TRACK_A.md` hasn't been touched this session.
   Today's work set the voice/safety/continuity floor + the
   continuity-ledger evidence layer; the readiness work is still
   the capital-T Track.

**Closed this session (no further action needed):**
- F1, F2, F3, F4, F5 from the autonomous-surface audit (all
  patched + regression-tested).
- chat_history threading regression (live-verified 14:25).
- README autonomous overwrite (stopped + 4 regression tests).
- CI-portable soul-path tests.
- Continuity ledger UTC/local-date timezone mismatch (fixed
  pre-fire, smoke ledger migrated to local-date filename).

---

## Sandbox patterns (unchanged from 2026-04-23 handoff)

1. `sudo` edits to `/etc/maez/*` or `/etc/systemd/*` are blocked
   for Claude Code's sandbox. Hand commands to owner, wait for
   "done."
2. `git push origin main` works from this machine for the owner's
   branch-protection config (verified this session — 6 pushes).
3. Always run the full CI-path ruff before pushing:
   ```bash
   .venv/bin/ruff check core/ skills/ daemon/ tests/ cli/ scripts/ memory/ hardware/ training/
   ```
4. **Don't restart `maez.service` yourself** if the owner is
   mid-test. This session's 3 restarts were all mid-conversation
   with explicit owner approval ("Restart Maez" / "Done with the
   restart").
5. The daemon imports modules at startup — a `core/` edit isn't
   live until `systemctl restart maez`.
6. Two Telegram entry points still in the tree:
   `skills/surface/maez_adapter.py` (active) and
   `skills/telegram_voice.py` (legacy). Verify which one the
   systemd unit wires up.

---

## Things I'd explicitly *not* have the next agent do without owner check-in

All items from the 2026-04-23 handoff still apply, plus:

- **Re-enable autonomous README regeneration.** The 4 regression
  tests in `tests/test_github_publish_no_readme.py` block silent
  re-introduction. A deliberate revert could still bypass — route
  README edits through deliberate commits only.
- **Bypass `audit_assistant_text` on any surface-write path.** The
  four bugs surfaced this session (README overwrite, return
  greeting, morning briefing, dream insight) all share that shape.
  Any new autonomous path that writes text to a user-visible
  surface needs explicit audit wiring from the start.
- **Change `_clean_exchange` or `history_to_messages` without
  running `tests/test_clean_exchange_forms.py` and
  `tests/test_conversation_history.py`.** Twelve regression tests
  cover the three stored-format shapes and the operational-note
  pass-through behavior. They're the contract boundary for
  continuity threading across all surfaces.

---

## If you need the Claude-side memory files

They live at `~/.claude/projects/-home-rohit/memory/` on this
machine. `MEMORY.md` there is the index. Most load-bearing for
continuing today's thread:

- `reference_maez_fabrication_source_priority.md`
- `reference_corrective_core_memory_pattern.md`
- `feedback_chat_self_claim_hallucination.md`
- `feedback_never_delete_maez_memory.md`
- `feedback_finish_experiment_before_reverting.md`
- `feedback_reject_repetition_loop_self_edits.md`

---

## Meta

This handoff was generated at the close of a continuous-work session
that spanned 2026-04-23 late evening → 2026-04-24 afternoon. The
owner treated "today" as still 2026-04-23 for most of the session;
the split across calendar dates is cosmetic, not a break.

If you're reading this more than a week later, trust `TRACK_A.md` and
`PROGRESS.md` over this doc — they're kept current. This file is a
snapshot of one handoff moment.
