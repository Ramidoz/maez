# Valence v0.1.1 Cadence Correction — Review Handoff

Status: ready for review. Branch: `valence-v0.1.1-cadence`.
**Lane note:** Claude implemented this (lane-swap). The independent covenant review must come from the OTHER lane (Codex or owner), NOT Claude self-review — it touches the live daemon heartbeat loop.

## What Changed

Live valence telemetry now reads only after `_cycle_doorman_gate.wake` is true. Quiet-skip loop ticks no longer call `_read_and_log_cycle_valence(...)`, so they append no neutral valence records.

- New helpers in `daemon/maez_daemon.py` (right after `_read_and_log_cycle_valence`): `_should_read_cycle_valence(gate_decision)` (returns `bool(getattr(gate_decision, "wake", False))`) and `_maybe_read_cycle_valence(daemon, *, gate_decision, open_wants_count, now)` (returns early unless wake; otherwise calls the existing read helper unchanged).
- The `_loop` tail call was replaced: `_read_and_log_cycle_valence(self, ...)` → `_maybe_read_cycle_valence(self, gate_decision=_cycle_doorman_gate, ...)`. Tail placement is unchanged (still after follow-up delivery, proactive opinion, dream check; before sleep).

## Review Anchors

1. The guard keys on the normalized `wake` field, NOT `doorman_enabled` — verified in code that both modes set it (legacy `wake=not legacy_skip` at `maez_daemon.py:2136`; doorman `wake=bool(verdict.wake)` at `2147`). So legacy gating reads on legacy-wake only.
2. Quiet-skip ticks do not call `_read_and_log_cycle_valence(...)` and therefore do not clear buffered audit flags.
3. The next wake drains buffered flags on a successful append (round-trip test).
4. Tail placement preserved for wake cycles (existing `test_structural_wiring_*` still asserts it, updated to the new call name).
5. No pure-valence files, record schema, retention policy, wants count, audit-buffer implementation, continuity reading, or doorman-decision logic changed. (`_count_cycle_open_wants` cap is untouched.)

## Deviations from the plan (please scrutinize)

1. **Source-order test newline.** The plan wrote the match strings with `\\n` (literal backslash-n). `inspect.getsource` returns *real* newlines, so the plan's string would never match (`assertNotIn` vacuous, `assertIn` always-fail). Verified empirically:
   - `'_read_and_log_cycle_valence(\n                self,' in src` → `True`
   - `'_read_and_log_cycle_valence(\\n                self,' in src` → `False`
   I wrote `test_loop_tail_uses_valence_cadence_guard` with a **real `\n`** so it genuinely matches the source (a comment in the test notes why). This is stronger than the plan, not weaker.
2. **Existing test update (necessary).** Renaming the loop call broke the pre-existing `test_structural_wiring_valence_read_runs_after_post_cycle_surfaces`, which located the read via `src.index("_read_and_log_cycle_valence(")` → `ValueError: substring not found`. One-line fix: `src.index("_maybe_read_cycle_valence(")`. Its placement-check intent (valence after follow-up/opinion/dream, before sleep) is unchanged.

## Verification (observed)

- Task 1 targeted RED → `AttributeError: module 'daemon.maez_daemon' has no attribute '_maybe_read_cycle_valence'` (4 tests).
- Task 1 GREEN → `Ran 4 tests ... OK` (incl. `test_cycle_valence_skip_then_wake_drains_buffer_on_success`).
- Task 2 source-order RED → `AssertionError: '_read_and_log_cycle_valence(\n                self,' unexpectedly found` (direct call still present).
- Task 2 source-order GREEN → `Ran 1 test ... OK`.
- Protected focused suite → `Ran 98 tests ... OK` (after the existing-test one-line fix; before it, 1 error = the broken existing test, now corrected).
- ruff → `All checks passed!`.

## Predicted Effect To Witness

After merge and owner restart, `logs/valence_telemetry.jsonl` should stop growing at the ~32s loop-tick cadence during quiet periods. New records should append only on doorman/legacy wake opportunities, including floor wakes. The live re-witness should confirm quiet periods no longer produce a record roughly every 32 seconds, while honesty/content-light/no-emotion guarantees remain.

## Not Done

- No merge.
- No restart.
- No live witness.

## Owner-Breath Sequence (after review passes)

1. merge (local fast-forward, no push);
2. owner restart;
3. live re-witness: confirm valence timestamps no longer land every loop tick during quiet, and entries track wake cadence.
