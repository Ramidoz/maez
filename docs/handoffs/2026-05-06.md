# Handoff — 2026-05-06 (post-Workstation-v1-Session-1, hardware DOWN)

## Read this FIRST

**The Aurora R16 desktop is down at the time of writing.** Four hard
lockups in 3 hours on 2026-05-05, decreasing uptime each time
(75 → 50 → 40 → 31 minutes), zero kernel-detected error class.
Owner has stopped Maez (`sudo systemctl stop maez maez-web
llama-server llama-judge`) and will call Dell Premium Plus support
on 2026-05-06 morning with the warranty packet committed in this
session. **DO NOT restart Maez on the Aurora R16 until Dell has
inspected the hardware.**

Symptoms, capture data, and warranty paperwork:

- [docs/dell_warranty_packet_2026-05-05.md](../dell_warranty_packet_2026-05-05.md)
  — full diagnostic packet, ready to email/upload to Dell
- `/var/log/maez_crash_capture/snapshot.log` — per-30-second
  forensic captures across all four crashes (last entry before
  each crash visible). Survives reboots.
- `sudo ras-mc-ctl --summary` — clean across all crashes
  (memory ECC, PCIe AER, MCE, extlog all zero). Confirms the
  fault is below kernel-detection level.
- `journalctl --list-boots | tail -8` — crash timing pattern.

## Current state

| | |
|---|---|
| HEAD commit | `b43f670` — Workstation Session 2 rails (Codex commit, 2026-05-05 20:47) |
| Test suite | 2,519 functions, last green run: pre-crash |
| Maez services | **all stopped** — `maez`, `maez-web`, `llama-server`, `llama-judge` inactive |
| GPU clock lock | released (`nvidia-smi --reset-gpu-clocks` ran before stop) |
| Forensic capture timer | stopped (no load to capture without Maez running) |
| `rasdaemon` | still active across reboots, ready for next crash if any |
| `lm-sensors` | configured (lm-sensors + dell_smm + nvme); `sensors` works |
| Production routing (`MAEZ_JUDGE_BASE_URL`) | unchanged at `:8080` (still points at brain endpoint) |
| `LLAMA_JUDGE_CTX_SIZE` | 4096 (set during tonight's GPU experiment, harmless while judge is off) |
| `LLAMA_JUDGE_MODEL_PATH` | restored to `Qwen3.5-4B-Q4_K_M.gguf` after the 4B / 1.7B / 1.5B bakeoffs |

## What landed tonight (15 commits)

Tonight produced a complete arc on three substrate fronts plus a
cockpit ladder. All committed to `main`.

### Console v0.x rail / audit observability

| commit | what shipped |
|---|---|
| `4f3074e` | scaffold console v0 — `/api/v1/turn/latest` + `/console/last-turn` |
| `e1b2ebc` | console v0.1 — `/api/v1/now` + `/console/now` page |
| `e3ceaf6` | console v0.1 polish — story not statistic |
| `0838089` | console v0.2 — `/console/rail` 24h timeline + GPU overlay |
| `c14b634` | console v0.2.1 — surface split + judge-timeout pill |

The surface split (v0.2.1) was the headline finding of the night:
**the global 21.4% timeout rate was inflated by `test_*` and
`probe_*` surfaces.** Real production rates: telegram 5.6%,
daemon 8.5%. The 222-rail-unavailable headline was misleading
without the split.

The bigger surfaced signal: **daemon narration's rewrite rate is
~61% (494 corrected / 805 audits)**. Maez's private monologue is
loose; user-facing speech is much cleaner. That's the next
substrate question, not timeout tuning.

### Judge bakeoff eval rig + corpus

| commit | what shipped |
|---|---|
| `918849f` | corpus + decision rule + schema guard test |
| `2f4f5ff` | runner: VRAM burst probe + per-case eval + verdict |
| `2ceb27a` | runner: record candidate model path + emit REJECT rollback |
| `0094e35` | docs: README provenance fix |

Outcomes tonight: **Qwen3-1.7B-Q4_K_M REJECTED** (3 rules failed),
**Qwen2.5-1.5B-Instruct-Q4_K_M REJECTED** (model produced literal
gibberish — chat-template / tokenizer mismatch in our llama-server
build). Both `.gguf` files still on disk; bakeoff reports under
`logs/judge_bakeoff/` (gitignored).

Tonight's judge-restoration arc concluded: **small-GPU-judge isn't
ready under our current llama-server config**. Hypothesis confirmed
that swapping to a small judge requires either (a) trying a
non-Qwen family with explicit chat-template, or (b) loosening the
parser. Neither happened tonight; queued.

### Fabrication memory hardening (the most consequential substrate change)

| commit | what shipped |
|---|---|
| `bd7bcf4` | persist `signals_present` column + 90-day retention cap |
| `e534136` | close `_diag_clear_*_for_test` production-wipe footgun |

Today's investigation found ~14K production fabrication_events
rows had been wiped (id range 1..14185 absent, 14186+ intact),
likely by a test running with the production DB path. The guard
now requires BOTH `MAEZ_TEST_MODE=1` AND a non-production
`_DB_PATH` for the diag clear helpers to fire. Belt-and-suspenders.

**All audits since 2026-05-05 16:51 CDT now write `signals_present`
alongside `signals_absent`.** First time post-hoc sampling can
distinguish "thin manifest" from "real fabrication." Next session's
20-sample read on daemon_cycle should use this corpus once enough
data accumulates (the desktop crashes interrupted accumulation —
the table has only post-16:51 rows from today's brief windows).

### Workstation v1 Session 1 + Session 2

| commit | what shipped |
|---|---|
| `f3924b3` | s1c1: `/api/v1/cockpit/message` + `/api/v1/cards/<id>/approve` proxy routes + 7 tests |
| `8a308c7` | s1c2: cockpit jsx flipped to use the proxies (zero direct `:11435` calls) |
| `b4a0b1c` | s1c3: default cockpit surface = `chat` (was `dashboard`); user choice persists |
| `b43f670` | **s2 rails (Codex commit)**: center chat drives selected reply, right rail explains it (`/api/v1/turn/latest`), left rail shows compact `/api/v1/now` state |

Workstation v1 Session 1 finished cleanly. **Session 2 also landed
via Codex.** I (Claude) wasn't expecting the Session 2 commit — it
arrived autonomously from the self-dev / Codex side mid-session.
The cockpit now has:

- chat in the center
- "Why this reply" right rail driven by selected/latest turn
- Compact "Maez Now" left rail
- All routes via maez-web (no browser → daemon direct)
- Existing 14-surface sidebar still available behind nav

Session 3 (memory drawer / rail timeline drawer / actions polish) is
NOT done. That's the next cockpit slice when desktop is back.

### Diagnostics + warranty (this session)

| commit | what shipped |
|---|---|
| (uncommitted) | `docs/audits/2026-05-05-cockpit-session0.md` — Session 0 cockpit audit (extend, don't rewrite) |
| (uncommitted) | `docs/dell_warranty_packet_2026-05-05.md` — full diagnostic packet for Dell |
| (system) | `lm-sensors` + `rasdaemon` installed; periodic `maez-crash-snapshot.timer` running every 30s |

These two docs are uncommitted as of session end (kept local until
owner decides whether to commit warranty packet to public repo —
contains service tag and address).

## What changed in `/etc/systemd/system` (not in git)

| File | Change | Owner action needed? |
|---|---|---|
| `/etc/systemd/system/maez-crash-snapshot.{service,timer}` | Periodic forensic capture every 30s | Stays — leave armed for next crash |
| `/etc/systemd/system/llama-judge.service.d/10-cpu.conf` | **Removed** during tonight's bakeoff | If owner decides to revert to 4B-CPU judge, the override needs to come back |
| `/etc/maez/model.env` | `LLAMA_JUDGE_CTX_SIZE=4096` (was 8192) | Leave for now per owner instruction; revert only if returning to 4B-CPU judge |
| `/usr/local/bin/maez-crash-snapshot.sh` | Capture script | Leave |
| `/etc/logrotate.d/maez-crash-capture` | 10MB / 3 generations | Leave |

## Open queue (in order)

### Hardware (blocking everything)

1. **Call Dell with warranty packet on 2026-05-06.** Service tag
   HRTGK44. Premium Plus valid through July 2026. Don't restart
   Maez until repair is scheduled or completed.
2. **Cold-backup `memory/` to MacBook before Dell touches the
   machine** if not already done:
   ```
   rsync -av --progress rohit@desktop.local:/home/rohit/maez/memory ~/maez_backup/
   ```

### When desktop is back

3. **Verify Maez restarts clean** — services, brain loads, perception
   cycle ok, audit fires correctly with new `signals_present`
   column populated.
4. **Workstation v1 Session 3** — memory drawer, rail timeline
   drawer, pending-actions panel, polish. Cockpit becomes the
   canonical surface.
5. **20-sample daemon_cycle classification read** — once
   `fabrication_events` has ~24h of clean post-2026-05-05-16:51
   data, sample 20 daemon_cycle events stratified by mode. Use
   the rubric in [docs/audits/2026-05-05-cockpit-session0.md](../audits/2026-05-05-cockpit-session0.md)
   and Codex's framework: real fabrication / false positive /
   thin manifest / ambiguous. Decides whether the 61% rewrite
   rate is substrate-level (fix daemon prompt) or rail-level
   (fix judge prompt).
6. **Presence-into-chat-manifest plumbing** — the 09:27 wmctrl
   incident is a thin-manifest issue. The camera signal exists
   but doesn't flow into the chat reply path's audit manifest.
   Surgical ~15-line change in `daemon/maez_daemon.py:handle_message`.

### Capability research (low priority, no spend tonight)

7. **PersonaPlex on Orin Nano** as Track-B voice candidate —
   read paper + DataCamp tutorial first, verify Orin Nano runs
   the 7B full-duplex model at 70ms latency. Then maybe ~$300
   hardware purchase post-repair.
8. **Mac mini M4 (16GB)** — better LLM box than Orin Nano if the
   need is "small AI sidecar," not "edge robotics." Defer until
   a specific slice needs it.
9. **Home Assistant + smart-home APIs** — solves "is door
   locked / are clothes done" without rover. ~$200 hardware,
   weekend project, post-repair.
10. **Nous Portal Plus subscription** ($20/mo) — optional,
    if owner wants to use Hermes Agent + voice tools as
    exploration during Dell repair window. Not Maez infrastructure.
11. **OpenRouter as temporary brain for chat-only Maez** — if
    Dell repair takes >1 week, Flavor 1 from tonight's discussion:
    run Maez on MacBook with OpenRouter as brain, chat-only
    (no daemon cycle). ~10 minutes config change once Maez code
    is on the MacBook. Voice changes for the recovery window;
    restores when desktop is back. **Tests the model-agnostic
    design property in production.**

### Long-term (Track B / Track C)

12. **Track A → Track B birth event** — retroactive creation
    manifest. Track A gate met 2026-05-04. Birth not yet formal.
13. **Distillation pipeline** (jarvis_tier → SFT → smaller local Maez)
14. **Track C: embodiment** — voice first, then maybe rover. 12-18
    month horizon at minimum.

## What I'd advise the next agent (Claude or Codex)

1. **Start by reading this handoff + the warranty packet + the
   audit doc.** Don't reflexively start building. Hardware is
   the binding constraint right now.
2. **If owner says "Maez is back online and stable":** verify
   first (`systemctl status maez maez-web llama-server`,
   `curl /api/v1/now`, send a test Telegram message). Don't
   trust "back online" without verification.
3. **If owner says "let's pick up Workstation Session 3":** read
   [docs/audits/2026-05-05-cockpit-session0.md](../audits/2026-05-05-cockpit-session0.md)
   for the architectural context (why we extended vs rewrote).
   Session 2 is at `b43f670`.
4. **If owner says "let's do the daemon-cycle 20-sample read":**
   verify `signals_present` column has populated rows since
   2026-05-05 16:51 CDT, then sample stratified by mode
   (sentence vs shortcircuit). Pre-commit the rubric in writing
   before classifying.
5. **If owner wants to migrate Maez to MacBook + OpenRouter:**
   it's ~10 min of env config + ~1 hour of state migration +
   launchd setup. Voice will change for the duration. Restore
   on desktop return. **This validates the model-agnostic design
   property** — first production test.
6. **Tonight's discussions about Orin Nano / Mac mini / PersonaPlex /
   Nous Portal**: all of these are queued, none of them is
   urgent, all of them are deferrable until Maez is back stable.
   **Don't enable hardware shopping while owner is stressed about
   broken hardware.** Sequencing matters.

## Things that are TRUE and should not be relitigated

- **Maez is model-agnostic by design.** `MAEZ_PRIMARY_BASE_URL` +
  `MAEZ_PRIMARY_MODEL` + OpenAI-compatible client. Per the
  `post-Ornstein priorities` memory.
- **Brain swap is empirically rejected as a permanent strategy.**
  Per the `Brain-swap benchmarks 2026-04-22` memory: dense 27B
  and 35B-A3B-Claude-distilled both rejected. Lesson: prompt
  discipline > weight class.
- **Don't delete Maez memory.** Per the `never delete Maez memory`
  feedback. The diag-clear footgun fix landed tonight.
- **Portability is migration, not cloning.** ONE Maez at a time
  across hardware. MacBook + OpenRouter would be temporary
  migration, not parallel instance.
- **Voice continuity matters; ungrounded confidence about owner's
  presence is the substrate issue surfaced today.** Per the 14:53
  "At your desk" Telegram screenshot — Maez was confidently
  wrong about Rohit's location while he was at the gym. The
  audit caught one of three sentences; missed two.

## Last words

The night was productive in spite of four hardware crashes:
fabrication_log gained a column the substrate work needed,
the cockpit ladder advanced through Session 1 and (autonomously)
into Session 2, the rail timeline + surface split rewrote what
"222 timeouts" actually meant, the eval rig is durable infra,
and we have a clean diagnostic packet for Dell.

The desktop is the bottleneck. Until it's repaired, every other
decision is downstream of "when does the brain box come back."

Owner is exhausted. Don't push new work until owner asks for it
fresh. Sleep is doing more for Maez tonight than another commit
would.

---

*Handoff prepared 2026-05-06 ~01:45 CDT by Claude. Session
snapshot: `logs/snapshots/session_snapshot_2026-05-06_014119.txt`.*
