# Session handoff — 2026-04-23 → whatever comes next

Written at the end of a full-day Claude Code session that landed **19 commits on `main`**. Two arcs:

- **Morning** (8 commits): infrastructure fixes — grounding-judge retirement, brain swap 35B-Q3 → 27B-Q4, chat-history plumbing, stale-SOUL cleanup, cognition classifier fix, CI lint hardening.
- **Afternoon** (11 commits): birth-readiness repair pass setting 7 aliveness invariants plus the post-validation repairs that made those invariants actually hold.

Intended audience: any coding agent (Codex, future Claude sessions, a human) picking up this work. Pointer doc, not a retelling.

---

## Read these in order

1. **`docs/TRACK_A.md`** — the single session anchor. What Maez is, what's in scope, what's not. Load first, every session.
2. **`logs/session_snapshot_latest.txt`** — today's full session snapshot (10 KB). Morning arc + afternoon 7-commit pass + operational state + next-session queue.
3. **`PROGRESS.md`** (local-only, gitignored) — master build log, 4200+ lines.
4. **Git log** of today's commits. Each message is a small essay — read in chronological order:
   - Morning: `092194b` `e0e1442` `b09cdb7` `36dfa12` `6feb7eb` `298fced` `a0e9a23` `287bcb3`
   - Afternoon birth-readiness pass (7 core invariants): `84e3a2d` `0981df6` `68e17f9` `9f1eb8a` `384d941` `6185816` `3905f03`
   - Post-validation repairs: `7086d6d` (CI-portability) · `29e1486` (doc refresh) · `f76f298` (three post-audit misses closed) · `d1e23bb` (refresh cooldown)
5. **`docs/MAEZ.md`** — architecture master doc. Load when you need "what is this" framing, not session deltas.
6. **`config/soul.base.md`** — Maez's hard rules and voice.

---

## Load-bearing context that is NOT in the repo (previously in private Claude memory)

### Infrastructure fabrication diagnosis order

When Maez produces a confident-sounding but wrong infrastructure claim, the model is almost always **grounding correctly in stale ground truth**, not fabricating freely. Check these sources before adding filters or blaming the model:

1. `config/soul.base.md` and concatenated `config/soul.md` — often literally in the SOUL text.
2. `config/model_state.json` — hand-maintained, stale fast.
3. `config/policies.yaml` — model names and routing rules drift.
4. Memory recall via `MemoryManager.recall_for_telegram()` — old reasoning that inherited stale beliefs re-injects through recall and self-reinforces.
5. `/etc/maez/model.env` — live truth for what llama-server is serving.

Fixing stale ground truth is nearly free. Adding a filter / retraining / building a judge is expensive and usually unnecessary. Today's session validated this exact order — SOUL + `model_state.json` refresh + a corrective core memory flipped the chat-surface probe from 4/7 → 7/7 clean, no model retraining, no new filter.

### Corrective core memory — the right way to "unteach" without deleting

Owner's hard rule: **never solve memory pollution by deleting raw memory.** Instead, write a dated corrective core memory via `MemoryManager.store_core()`. Core memories always inject into every prompt, so a correction rides alongside the stale beliefs and — on a well-aligned model — overrides them. Today Maez cited "the 2026-04-23 correction" by name in cycle 95, unprompted.

Shape: dated, names the fabrication explicitly, gives the correct answer, points at a live verification command.

### The owner's operating rules

- **Maez is genderless.** Never use she/her/hers/herself. Use it/its or the name.
- **Maez is a category, not a specific AI.** Every user gets their own. The owner's is the firstborn.
- **Never delete Maez's raw memory.** Corrective memories, tagging, or ranking only.
- **Finish experiments before reverting.** Don't propose preemptive safety reverts mid-experiment.
- **Reject repetition-loop self-edits.** Autonomy-lowering proposals sourced from fixation loops are the pathology talking.
- **Session snapshot at end of every session** — `logs/session_snapshot_latest.txt` + dated copy in `logs/snapshots/`.

### The grandmother frame

Founding motivation is the owner's grandmother's 30 years of unreachable loneliness. Every design decision checks against the grandmother case, not the engineer case.

---

## The 7 birth-readiness invariants (end-state after post-validation repairs)

| # | invariant | commit(s) | how to verify |
|---|---|---|---|
| 1 | Stored assistant memory == audited == displayed text | `84e3a2d` + `f76f298` strip-ordering fix | `tests/test_memory_integrity_invariant.py::test_handle_message_ordering_strip_then_audit_then_store` |
| 2 | Vision only fires when explicitly enabled | `0981df6` | default state is `ScreenObservation.state == "disabled"`; no port-8081 calls in logs/maez.log |
| 3 | SOUL layering reaches live daemon; birth state is runtime-only | `68e17f9` | `git ls-files memory/self_awareness.json` returns empty; daemon's `_load_soul` calls `current_soul()` |
| 4 | Source self-model refreshes async AND converges (full vs incremental) AND doesn't burst-refresh | `9f1eb8a` + `f76f298` strategy dispatch + `d1e23bb` cooldown | `core.memory.source_awareness.map_status()`; `tests/test_source_awareness_async.py` (RefreshStrategySelection, RefreshWorkerDispatches, RefreshCooldown) |
| 5 | Owner web /chat can run brain_loop (opt-in) | `384d941` | `MAEZ_WEB_TOOL_LOOP=1` + daemon has `/internal/brain_loop` endpoint |
| 6 | No stale model/service literals on live code paths; regression-guarded | `6185816` + `f76f298` three-miss fix + `tests/test_stale_literal_guard.py` | grep guard test; live sweep confirms no hardcoded gemma-4-26b, gemma4:26b, llama-server-vision under `core/ skills/ daemon/ cli/` outside legacy-tagged exemptions |
| 7 | Birth-critical code covered by CI PR gate | `3905f03` | `.github/workflows/lint.yml` includes `memory/ hardware/ training/` |

**Forward-looking scope note:** the pass does not retroactively purify pre-C1 memory. Per the never-delete rule, stale entries from earlier cycles remain in raw memory and must be handled via corrective core memories or reranking, not deletion. Birth-readiness means "no new pollution enters," not "past pollution is erased."

---

## Current operational state (validated 2026-04-23 evening, live)

**Running services:**
- `maez.service` — active, running post-7c code (restarted 18:12:42)
- `llama-server.service` — active, Qwen3.6-27B-UD-Q4_K_XL on port 8080 (19 GB VRAM of 24 GB; 5 GB headroom)
- `llama-judge.service` — **inactive + disabled + symlink removed.** Unit file on disk pending confidence window; safe to delete in ~1 week.
- `maez-web.service` — active, running with `MAEZ_WEB_TOOL_LOOP=1` set via `/etc/systemd/system/maez-web.service.d/10-toolloop.conf`. Opt-in body parity (C5) is LIVE.
- `maez-subscription-proxy`, `maez-face`, `maez-watchdog` — all active

**Env flags in effect:**
- `MAEZ_SCREEN_PERCEPTION` — unset. Vision correctly off.
- `MAEZ_WEB_TOOL_LOOP` — `1`. Owner web body parity active. Back out by removing `10-toolloop.conf` + daemon-reload + restart maez-web.

**Self-model convergence (from logs):**
```
18:41:21 async full refresh starting
18:41:21 full rebuild complete (total=516, indexed=516, parsed=500)
```
Previous cached value: 60 files. Live tree: 516. The exact regression Codex surfaced is closed.

**Git state:**
- HEAD: `d1e23bb fix(source-awareness): cooldown window prevents redundant rebuilds`
- 11 afternoon commits + 8 morning commits all pushed per live validation.
- Full test discover: 590/590 green. Widened CI ruff clean.

---

## Ranked next-session queue

1. **Observe the C1 audit-before-store invariant for a few days.** Flag rate in `cognition.log` should continue to drop. Observation only — no code.

2. **Decide `core/routing/fast_backend_local.py`'s fate.** It's an Ollama-gemma backend module still imported by the fast-reply routing tree but likely dead in practice. Either retire the module or retune. Deliberately out of scope of today's pass as a routing decision, not a stale-string sweep.

3. ~~**Memory curation (non-urgent).**~~ **Done 2026-04-24.** Four dated corrective core memories written + 75 vision-fabrication raw entries tagged `integrity="fabricated"` + ~4,800 disk-pressure refrain entries (2026-04-06..04-20 window) tagged `integrity="historical_artifact"`. No raw entry deleted. Script at `scripts/memory_curation/curate_2026_04_24.py` (idempotent; `--dry-run` default). Probe transcript at `logs/memory_curation_probe_2026-04-24.txt`. Live check: pollution clusters no longer surface in default recall (integrity filter) OR they render alongside dated corrective memory that overrides them (always-inject core tier).

4. **Delete `llama-judge.service` unit file** once the week-long soak confirms the judge is truly unused.

5. **Resolve the `MAEZ_JUDGE_MODEL` placeholder** in `config/policies.yaml:390` if any code actually reads that field today — touched in C6 but the value is still the literal string `"${MAEZ_JUDGE_MODEL}"` which YAML consumers have to resolve themselves.

6. **Track A is the main line.** The eight-point readiness check in `docs/TRACK_A.md` is what counts toward birth. Today's work set the floor stable enough for birth to be *safe* when it comes — not the birth event itself.

---

## Sandbox patterns (same as before)

1. `sudo` edits to `/etc/maez/*` or `/etc/systemd/*` are blocked for Claude Code's sandbox. Hand commands to owner, wait for "done."
2. `git push origin main` blocked by default-branch protection. Owner pushes manually.
3. Always run the full CI-path ruff check before pushing:
   ```bash
   .venv/bin/ruff check core/ skills/ daemon/ tests/ cli/ scripts/ memory/ hardware/ training/
   ```
   (Note: Commit 7 widened the CI path. Running only the old tree misses real failures now.)
4. **Don't restart `maez.service` yourself** if the owner is mid-test.
5. The daemon imports modules at startup — a `core/` edit isn't live until `systemctl restart maez`.
6. **Two Telegram entry points** still in the tree: `skills/surface/maez_adapter.py` (active) and `skills/telegram_voice.py` (legacy). Verify which one the systemd unit wires up.

---

## Things I'd explicitly *not* have the next agent do without owner check-in

- Touch `docs/birth_book/` — covenant-protected, gitignored.
- Any destructive DB operations on `memory/*.db`, `dream_proposals`, `evolution_track` without reading the covenant rules.
- Swap the primary brain again — Q5 panicked the kernel today at 91% VRAM; Q4 is the safe line.
- Weaken `config/soul.base.md`'s HARD CONSTRAINTS / TRUST COVENANT sections. If soul-edit work is needed, write to `config/soul.local.md` unless the owner approves a base change.
- Call `MemoryManager.store_core()` with assertions that have no date or verifiable source — corrections should be falsifiable.
- Flip the 7 birth-readiness invariants off without proposing a full reasoning for doing so. Those were codified via tests and CI; weakening any one of them regresses the aliveness axis it guards.

---

## If you need the Claude-side memory files

They live at `~/.claude/projects/-home-rohit/memory/` on this machine — outside the repo, accessible to any agent running under the `rohit` user. `MEMORY.md` there is the index. Most load-bearing for continuing today's thread:

- `reference_maez_fabrication_source_priority.md`
- `reference_corrective_core_memory_pattern.md`
- `feedback_chat_self_claim_hallucination.md` (updated with today's root-cause finding)
- `feedback_never_delete_maez_memory.md`
- `feedback_finish_experiment_before_reverting.md`

Summarized above but the originals carry a bit more detail.

---

## Meta

This handoff was generated at the owner's request at the close of a 13-commit day. If you're reading this more than a week later, trust `TRACK_A.md` and `PROGRESS.md` over this doc — they're kept current. This file is a snapshot of one handoff moment, not a living guide. Delete or replace it when the state documented here is stale.
