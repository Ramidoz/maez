# Sandbox-Witness Contract — Codex Pass-2 Review — Lovelace / Bernoulli

**Artifact reviewed:** `docs/slices/sandbox-witness-contract/spec-brief.md` v1.2  
**Pass-2 brief:** `docs/slices/sandbox-witness-contract/reviews/codex-engineering-pass2-brief.md`  
**Verdict:** STILL OPEN

| Batch | v1.2 change cited | Verdict | Evidence |
| --- | --- | --- | --- |
| 1 | Lines 264-279, 403-404 | CLOSED | Legacy witness is read-only compatibility, renamed `legacy_sandbox_witness_json`, refused at append/update/emit/ratify with `LEGACY_WITNESS_SHAPE_REFUSED`, and covered by boundary + static tests. |
| 2 | Lines 61-68, 270-273, 399 | CLOSED | `witness_id` / monotonic generation is identity; `(bond_id, proposal_id)` is index; re-witnessing appends; stale/current preservation has W#12. |
| 3 | Lines 71-78, 311-323, 400-402 | CLOSED | Ratification has lock order and final eligibility snapshot binding proposal, witness generation, anchors, reverify result, ack id, `WitnessStatus`, and preference/status transition. Stale ack/concurrent advancement tests named. |
| 4 | Lines 109-114, 191-214, 373-379 | STILL OPEN | v1.2 closes file hashes, existence/deletion, mtime-as-cache-only, per-locus DB cursor tuples, monotonic/change-table authority, and diagnostic truncation/rotation/writer identity. But pass-2 closure requires a WAL/concurrency test. The RED anchors cover secondary append and update/delete without cursor at line 378, but no WAL/concurrent writer fixture is named. Closure criterion: add an explicit W#5* anchor for DB cursor behavior under WAL/concurrent reader-writer interleaving, or explicitly fold WAL/concurrency into W#5e's assertion text. |
| 5 | Lines 101-106, 163-174, 369 | CLOSED | v1.2 now makes `observed_effect = f(artifacts)` normative, uses `hmac-sha256(canonical_json(projection))`, excludes nondeterministic fields, sorts where ordering matters, defers `WORKTREE_BEHAVIORAL`, and parameterizes W#4a over every populated kind. Dry-run observation is narrowed to durable source/cursor or refused. |
| 6 | Lines 115-120, 246-260, 380-383 | CLOSED | Field families cover ids, bond id, enums, digests, refs/paths, narrative content, and technical ids. Digest provenance and `_is_digest` are required; W#6 is table-driven and crosses real boundaries including refs, paths, test names, evidence kinds, and producer ids. |
| 7 | Lines 121-128, 384-389 | CLOSED | v1.2 requires canonical module identity by resolved path, `sys.modules` alias/shim normalization, forbidden producer/verifier edges, dynamic import/reflection restrictions, runtime provenance tags, and fixtures for shim/importlib/`__import__`/`getattr`/shared helper laundering. |
| 8 | Lines 133-139, 390-394, 409 | CLOSED | Subprocess isolation is bound before imports, uses substrate-root/path-helper semantics, defines precedence requirement, uses `close_fds=True`, asserts registered paths resolve under scratch root, and tests live FD/path/import-time behavior. |
| 9 | Lines 232-245, 390, 393-394 | CLOSED | `SubstrateLocus` is explicit for v1, registry must map every path helper and hardcoded `memory/*.db` default, and unregistered opens refuse by default. |
| 10 | Lines 176-189, 396-407 | STILL OPEN | v1.2 lists refusal reasons and says W#10 is table-driven over every reason, but it does not provide the required per-reason boundary mapping. Line 407 only names boundary categories generally; it does not bind each reason to exactly one exercised boundary. Closure criterion: add a refusal-path matrix mapping every `WitnessRefusalReason` to construction / attachment / re-verification / ratification-time recheck / migration write-boundary, with the intended fixture for each. Divergence remains correctly non-refusal. |
| 11 | Lines 328-334, 409 | STILL OPEN | The cost split itself is closed: attach-time full subprocess re-verification, ratify-time indexed freshness/locus/generation check, optional `FULL_RERUN_AT_RATIFY` deferred, expected costs named. But pass-2 requires tests that count subprocess use and prove ratify-time freshness without accidental rerun. Line 409 names cost expectations but no W# anchor explicitly counts subprocess invocations. Closure criterion: add a W#11*/W#13* test requiring attach-time subprocess count = 1 and ratify-time subprocess/full-suite count = 0 unless `FULL_RERUN_AT_RATIFY` is explicitly enabled. |

The core deterministic artifact-function fold, including Batch 5, is materially closed. The remaining engineering gaps are test-spec precision gaps: WAL/concurrency coverage for staleness, explicit per-reason refusal-boundary mapping, and subprocess-count proof for the attach/ratify cost split.

