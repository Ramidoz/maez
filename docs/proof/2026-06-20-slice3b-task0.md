# Slice 3b — Task 0 Proof Gate (Beta belief, shadow-compared)

**Date:** 2026-06-21. **Verdict: GO.** Branch `earned-maturity-slice3b`. Controller-verified (owner calibration: Task 0 light).

## HARD SEAM 1 — the consult guard to EXPAND (PASS)

Existing Slice-1/3a consult guard at [maez_daemon.py:5909-5910](../../daemon/maez_daemon.py#L5909): `if os.environ.get("MAEZ_ROUTING_PRIORS_SHADOW") == "1" or os.environ.get("MAEZ_ROUTING_PRIORS_ENABLED") == "1":`. The block imports `from core.routing.observation import _default_store` (5911 — **confirms the plan keeps it**), `learn_priors`, `classify_request_class`; computes `_cls`/`_prior`; logs `routing_prior_shadow`. Task 2 changes the guard to `_routing_prior_consult_enabled()` (4-flag OR) so `MAEZ_ROUTING_BETA_SHADOW=1` alone reaches the consult (false-witness fix), keeps `routing_prior_shadow` on its own flags, adds the Beta sub-block on the Beta flags. The veto application is below (`_reflex = needs_web_search(text)` at 5920 → the 3a `_would_web_search`/override → the `if PRIORS_ENABLED and _prior_vetoes_reflex(_prior) and _override_event_id is None: _reflex=False`). Task 2 inserts `_veto_decision = _prior_vetoes_reflex(_prior)` + the flag-gated Beta swap before it, and changes the application to `... and _veto_decision ...` — PRIORS_ENABLED stays the authority lead.

## HARD SEAM 2 — scipy verdict statistic (PASS)

`beta_would_veto = beta.cdf(max_success, prior_alpha+usable, prior_beta+(n-usable)) >= credence`. Defaults `prior_alpha=1, prior_beta=1, max_success=0.4, credence=0.9, min_observations=3, n8_min_conf=0.6`. scipy 1.17.1 available; lazy-imported ONLY inside the Beta sub-block. Live anchors (`scipy.stats.beta.cdf`) confirm the calibration table EXACTLY:

| case | n | usable | n8_conf | n8_veto | beta_p | beta_veto |
|---|---|---|---|---|---|---|
| thin-2 | 2 | 0 | 0.000 | False | 0.784 | False |
| 3-streak | 3 | 0 | 0.375 | False | 0.870 | False |
| 4-streak | 4 | 0 | 0.500 | **False** | 0.922 | **True** ← diverge (TUNING WARNING, not a win) |
| 5-streak | 5 | 0 | 0.625 | True | 0.953 | True |
| mixed-2of5 | 5 | 2 | 0.625 | **True** | 0.456 | **False** ← KEYSTONE (gate 5): n8 overclaims, Beta abstains |
| useful-5 | 5 | 5 | 0.625 | False | 0.004 | False |

## Scope (PASS)

Only `priors.py` (add `beta_belief`/`compare_beliefs`/`BeliefComparison`; n/8 `learn_priors`/`_confidence` UNCHANGED) + the 2 daemon seams + tests + docs. The n/8 veto path stays live; both Beta flags default-off = byte-identical. Hardcoded `prior/credence/thresholds` are 3b scaffolding (3c earns them) — named in the plan.

**GO** — seam + scipy + the five-gate anchors confirmed; keystone (mixed) and the 4-streak tuning-warning both reproduce.
