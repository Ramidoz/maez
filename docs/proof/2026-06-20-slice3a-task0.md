# Slice 3a — Task 0 Proof Gate (veto-event ledger seams)

**Date:** 2026-06-20. **Verdict: GO.** Branch `earned-maturity-slice3a`. Controller-verified (owner calibration: Task 0 light).

## HARD SEAM 1 — record point (PASS)

The veto fires at [maez_daemon.py:5903](../../daemon/maez_daemon.py#L5903) (`_reflex = False` when `MAEZ_ROUTING_PRIORS_ENABLED=1 and _prior_vetoes_reflex(_prior)`). In scope there:
- `_cls` ([:5896](../../daemon/maez_daemon.py#L5896)) and `_prior` ([:5897](../../daemon/maez_daemon.py#L5897)) — but computed only inside the `SHADOW or ENABLED` try block, so **`_cls = None` must be initialized before** (must-fix 3).
- `_user_msg_turn_id` ([:5659](../../daemon/maez_daemon.py#L5659), `None` fallback :5670) — the ledger turn_id.
- `source` — the surface (used at the record call `surface=source` :5951).
- `_prior.n` / `.success_rate` / `.confidence` — `RoutingPrior` fields (Slice 1); `_prior` is non-None whenever `_prior_vetoes_reflex(_prior)` is True.

## HARD SEAM 2 — `_would_web_search` conditions (PASS — must-fix 1)

The real search gate ([:5905-5912](../../daemon/maez_daemon.py#L5905)): `not authoritative_tool_reply and _daemon_parallel_web_search_enabled(transcript, recall_stack_config=_recall_stack_config) and _reflex`. `_would_web_search` (Task 2 Step 4) **MUST mirror exactly these** so a veto is recorded only when a search would otherwise have fired. In scope before the gate: `authoritative_tool_reply` ([:5805](../../daemon/maez_daemon.py#L5805)), `_recall_stack_config` ([:5881](../../daemon/maez_daemon.py#L5881)), `transcript` (handle_message param), `_reflex` (:5902, pre-veto = `needs_web_search(text)`). The override (seam 2) runs after `_reflex` is set and before the veto application (5903).

## HARD SEAM 3 — classify point (PASS — must-fix 2)

The override turn's calibrated outcome variable is **`_q`** ([:7230](../../daemon/maez_daemon.py#L7230), from `_routing_quality_from_gate(...)`): `None` when the reach was adequate, `"unusable"` when caveated/thin. The insert-time outcome is set at the record call ([:5950](../../daemon/maez_daemon.py#L5950), `outcome_quality=("structured_evidence" if _routing_obs_count > 0 else "empty_but_honest")` — implementer binds the exact expression). So `_routing_turn_outcome_quality` = insert-time value (set at 5950) **overridden by `_q`** when `_q is not None` (set at 7227). Classification fires only when `_override_event_id is not None AND _routing_turn_outcome_quality is not None` — **never a default** (`_q in dir() else "structured_evidence"` is removed).

## Window / lazy uncontested / scope (PASS)

`_REASK_WINDOW_S = 3600` (1h) — a "what counts as a re-ask" definition, NOT a trust knob. `uncontested` resolved **lazily** in `VetoLedger._resolve_expired` on ledger read (no scheduler). Scope: only `core/routing/veto_ledger.py` (new) + the 3 daemon seams + tests + docs. Untouched: strict honesty gate, S7, Telegram, time-sense, cockpit-reauth.

**GO** — all three hard seams confirmed; the writeback outcome variable is `_q` (the plan's reference is the real name).
